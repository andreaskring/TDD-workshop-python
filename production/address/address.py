def fetch_address(cpr: str) -> str:
    """
    We imagine that this function will do something complicated, i.e.
    such as sending the CPR number to a server and receive the
    address in return. This function should NOT be called from
    person.py, but it should instead be mocked.
    :param cpr: The CPR number of the person.
    :return: The address of the person.
    """

    return "Åbogade 15, 8200 Aarhus N"
